---
name: Feature request
about: Something the tool should be able to do
title: ""
labels: enhancement
---

**What do you want to do that you can't today?**

<!-- Describe the task, not the solution — e.g. "see which services have auto
deploy on" rather than "add a column". -->

**Does EasyPanel's own API support it?**

<!--
If you know the endpoint, name it. This tool deliberately only exposes what the
API actually does — it never invents a schema or guesses a response shape — so a
feature that needs an endpoint the API doesn't have can't be built. If you're not
sure, that's fine; say so.
-->

**How would you expect it to look — a CLI command, a TUI screen, or both?**

**Anything else**
